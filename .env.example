# Frontend Environment Variables (copy to .env and fill real values)
# Base URL of your backend API (no trailing slash)
VITE_BASE_URL=http://localhost:4000/api/v1

# OpenAI API key (if features use OpenAI on the frontend)
VITE_OPEN_AI_KEY=sk-your-openai-key
